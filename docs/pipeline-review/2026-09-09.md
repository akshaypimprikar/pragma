---
date: 2026-09-09
addressed: true
---

# Pipeline Review — 2026-09-09

## Critical
(none)

## High
(none)

## Medium
- [x] `trim-context.md`'s template copy has an entire "### 5. Runtime read/tool-output waste (headroom audit-reads)" section (`trim-context.md:48-58`, the `headroom audit-reads` diagnostic) that FinanceTracker's live copy of `trim-context.md` does not have — FinanceTracker's `/trim-context` is missing a diagnostic already added to the template. — **Fix:** back-port the section-5 block into FinanceTracker's `.claude/commands/trim-context.md` (adjust the `--path` example to FinanceTracker's actual project path) so the live pipeline gets the same diagnostic pragma already documents. **Done 2026-09-09.**
- [x] `review.md` has two whole sections in the template that FinanceTracker's live `review.md` lacks entirely: "## A known tradeoff: context continuity, not context isolation" (`review.md:73-80`) and "## Tip — automate the review-fix loop" (`review.md:96-102`, the `/loop 5m` babysit pattern). Both read as deliberate additions to the template's documentation of `/review`'s design, never pulled into the live project's copy. — **Fix:** copy both sections verbatim (they're project-agnostic, no placeholders needed) into FinanceTracker's `.claude/commands/review.md`. **Done 2026-09-09.**
- [x] Memory `project_skills_to_introduce.md` (last modified 2026-08-01) and MEMORY.md's index line for it both still describe the `fewer-permission-prompts` auto-prune extension ("auto-pruning settings.json junk on PR create/session end") as an outstanding gap — but FinanceTracker PR #99 (merged 2026-08-18, confirmed via `gh pr view 99`) already shipped it: "First run of the new `fewer-permission-prompts` prune mode... Adds two nudge hooks (`PreToolUse` on `gh pr create`, `SessionEnd`)." The described gap has been closed for three weeks. — **Fix:** update `project_skills_to_introduce.md` (and the MEMORY.md index line) to mark the auto-prune extension as done, citing PR #99. **Done 2026-09-09.**
- [x] Memory `feedback_design_asset_signoff_before_commit.md` cites FinanceTracker PR #99 as the incident where an app icon was committed without sign-off ("Generated the FinanceTracker app icon... directly into PR #99"). PR #99's actual merged body explicitly reverts that: "~~Adds AppIcon assets~~ — reverted. Several generated concepts were tried and reviewed but none were a good fit; the app icon is deferred to a separate pass rather than shipped here." The PR citation no longer matches git history — the icon never actually shipped in #99. — **Fix:** correct the memory's citation (verify against the actual PR where an icon shipped without prior sign-off, if one exists, or note that PR #99 itself self-corrected before merge and find the right supporting example). **Done 2026-09-09** — clarified the memory to note the icon was committed then reverted before merge, so the lesson (commit before sign-off is the mistake) still holds accurately.
- [x] MEMORY.md's index entry for `project_linkedin_series_state.md` ("18 published (Merge Rule July 17); Reuse/Actor Isolation queued for Tue/Wed; Post 8 + Haiku still blocked") is stale relative to the topic file it summarizes: the file itself (modified 2026-08-11, more recent than the index line implies) already shows 23 published posts, including "The Reuse Suggestion That Broke Actor Isolation" as published July 22 — not "queued." — **Fix:** regenerate the MEMORY.md index line from the topic file's current content next time either is touched. **Done 2026-09-09.**

## Low
- [x] pragma's `release.md` template has no equivalent of FinanceTracker's "### 5. Verify the release branch only touches release files" step (`release.md:70-76` in FinanceTracker's copy) — a guard that confirms a release-branch diff only touches `project.pbxproj`/`CHANGELOG.md`/`feature-log.md` before opening the PR, since release PRs are exempt from `/review` and `code-review:code-review` on the assumption they carry no logic changes. This is a real safety check present downstream, not yet pulled up into the template. — **Fix:** back-port a templated version of this step (using `<AppName>.xcodeproj/project.pbxproj`, `CHANGELOG.md`, `.claude/context/feature-log.md` as the allowed-paths list) into pragma's `release.md`. **Done 2026-09-09.**
- [x] pragma's `sync-workflow.md` template has no equivalent of FinanceTracker's "### 5. Self-review the diff before committing" section (`sync-workflow.md:37-71` in FinanceTracker's copy — three sub-checks: no FinanceTracker-specific literals leaked into template content, `<placeholder>` convention held, gate numbering/counts internally consistent) nor its generalizable-vs-app-specific judgment call for new gates (FinanceTracker `sync-workflow.md:33-41`) nor its "verify before trusting the PR" `--head`-flag tip (FinanceTracker `sync-workflow.md:89-93`, which directly reflects the confirmed `feedback_gh_pr_create_head_flag` incident from pragma PR #32). This is the exact kind of already-fixed logic the `/pipeline-review` process itself calls out as most important to catch, and it hasn't been pulled up. — **Fix:** back-port this self-review section and the `--head` verification tip into pragma's own `sync-workflow.md`, since `/sync-workflow` only pushes project → template and would otherwise never catch a gap in its own file. **Done 2026-09-09.**
- [x] Gate numbering/ordering has diverged between pragma's `gates.md` (Gate 8 = Abstraction bloat, Gate 9 = RED-before-GREEN, Gate 10 = Architecture compliance) and FinanceTracker's `gates.md` (project-specific Gate 8 = CSV import concurrency shape, Gate 9 = Architecture compliance, Gate 10 = Abstraction bloat, Gate 11 = RED-before-GREEN). Each file is internally self-consistent (`parallel-review.md`, `plan.md`, and `review.md` in each repo correctly reference their own repo's numbering) so this isn't a live bug, but the shared gates' relative order and count have drifted, purely from FinanceTracker inserting its own gate ahead of the shared ones instead of after them. — **Fix:** low priority; if `/sync-workflow` is ever run for `gates.md`, decide on one canonical ordering (project-specific gates last, matching the template's convention) and reconcile. **Deferred 2026-09-09** — deliberately left open, not a live bug; renumbering across both repos' cross-referencing command files is a real-risk change not worth rushing into this batch.
- [x] `scaffold/.github/workflows/release.yml` includes `-enableCodeCoverage YES` on the release-branch unit-test run; FinanceTracker's copy of the same workflow has dropped that flag. Could be an intentional FinanceTracker-side simplification (coverage already enforced elsewhere) or an accidental drop when the workflow was last hand-edited — unconfirmed either way. — **Fix:** confirm with Akshay whether coverage should run in the release workflow; align whichever file is wrong. **Resolved 2026-09-09** — it WAS intentional: commit `82a246c` in FinanceTracker already investigated this and confirmed locally that coverage collects fine without the flag (89.8% on `BalanceService.swift`), deliberately not porting it. An initial fix attempt added the flag to FinanceTracker anyway (based on an answer to a question that didn't surface this prior investigation) and was reverted before merge once `code-review:code-review` caught the conflict against git history. No change needed in either file — pragma's scaffold keeps the flag as a safe generic default for consumers whose scheme doesn't already collect coverage; FinanceTracker correctly omits it.
- [x] FinanceTracker's `.github/workflows/ui-tests.yml` adds `-parallel-testing-enabled NO` to its UI test invocation that `scaffold/.github/workflows/ui-tests.yml` doesn't have — reads like a learned fix for simulator contention during parallel UI test runs, not yet back-ported to the scaffold template. — **Fix:** add `-parallel-testing-enabled NO` to `scaffold/.github/workflows/ui-tests.yml` unless there's a reason it's FinanceTracker-specific. **Done 2026-09-09.**
- [x] Two dangling internal memory cross-references point at files that no longer exist in the memory store: `[[project_pipeline_followups_pending]]` (referenced from `feedback_gh_pr_create_head_flag.md:13`) and `[[feedback_gh_pr_base_develop]]` (referenced from `feedback_visuals_in_pr_descriptions.md:21`). Both read as unqualified links (not hedged like the one legitimate conditional link found, `[[project_pragma_first_release]]` in `feedback_proactive_release_reminders.md:18`, which already says "if that memory exists"). — **Fix:** update or remove these two links next time either file is touched; low priority since they don't block anything, just dead-end a reader following the trail. **Done 2026-09-09.**

## All clear
- Stale skill references (check 1): every skill/command invocation found across all 18 files in `.claude/commands/` (`code-review:code-review`, `simplify`, `ios-coverage`, `security-review`, `loop`, plus every self-referential `/plan`, `/spec`, `/design`, `/review`, `/feature`, `/test`, `/bugfix`, `/release`, `/gates`, `/pipeline-review`, `/sync-workflow`, `/trim-context`, `/status`, `/benchmark`, `/parallel-review`, `/pr-followup`, `/pragma-review`, `/init`) is on the current registry. Other `/word`-shaped strings found by grep (`design-system.md`, `benchmark-log.md`, `capture_pipeline_metrics.py`, `check_tdd_commit_order.py`, `deterministic-pr-gates` under `skills/`) are doc/script/local-skill references, not stale registry entries. `pragma:init` and `pragma:pragma-review` are the plugin-prefixed install-time names for `init` and `pragma-review`, already correctly documented as pragma-repo-only in `init.md:27`.
- CLAUDE.md token budget (check 3): not applicable — pragma has no `CLAUDE.md` of its own; it is the scaffold that generates one into consumer projects (`init.md` step 4), not an app.
- Pipeline gate coverage (check 5): all seven required gates exist at their required locations in pragma's own command files — Pre-PR gate (`gates.md`), build verification separate from tests (`gates.md:27` Gate 1 vs. `gates.md:35` Gate 2), CHANGELOG incremental update (`feature.md`'s GREEN-commit step), `/review` before `/test` ordering (`feature.md`'s Done-when: `/review` runs first via `/pr-followup`, then `/test` and `code-review:code-review`), coverage check (`gates.md:67` references the `ios-coverage` skill), security check (`gates.md:75` references the `security-review` skill), session recovery (`status.md`).
- Command file completeness (check 6): all 18 files in `.claude/commands/` have Trigger, Process, and Done when sections — no gaps found.
- Settings hygiene (check 7): not applicable — pragma has no `.claude/settings.json` of its own (only `.claude/context/` and `.claude/commands/` exist under `.claude/`), and `scaffold/` contains only `.github/workflows/` — no `scaffold/.claude/` directory exists at all, so there is no scaffolded `settings.json` to audit either. `init.md`'s Process confirms `.claude/commands/` is copied directly from the plugin root, not from a scaffold settings template — this is by design, not an omission.
