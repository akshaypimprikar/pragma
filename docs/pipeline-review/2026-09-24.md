---
date: 2026-09-24
addressed: true
---

# Pipeline Review — 2026-09-24

## Critical
(none)

## High
- [x] Template drift (check 2), app-specific content in the template: `test/SKILL.md:32` hardcodes FinanceTracker's own UI flows as the coverage target for every consumer ("cover critical happy paths: add transaction, import CSV, budget alert"). FinanceTracker's copy has the same line, so it was carried over unchanged, not generalized. — **Fix:** replace it with a placeholder, e.g. `cover critical happy paths: <your app's 2–3 core user flows>`. **Fixed 2026-09-24** — placeholder `<your app's 2–3 core user flows>`.
- [x] Template drift (check 2), app-specific content in the template: a finance-domain rule is written as a universal architecture rule in three skills: `feature/SKILL.md:53` ("Money values: `Decimal`, never `Double`"), `plan/SKILL.md:49` (same rule), and `spec/SKILL.md:91` ("All money values use `Decimal`, never `Double`"). `review/SKILL.md:99` adds "No magic numbers for monetary thresholds." pragma itself treats this rule as conditional elsewhere: `init.md:61` suggests money-as-Decimal only "if it's a finance-adjacent app", `gates/SKILL.md:192` shows it as an "e.g." example, and the scaffolded AGENTS.md (`scripts/setup.sh`, Key constraints block) lists it only as a commented-out example. A non-finance consumer gets a rule that `/spec`/`/plan`/`/feature` say to "enforce in every" spec/task. — **Fix:** in feature/plan/spec, replace the line with a placeholder such as `<type-safety rule from AGENTS.md, e.g. money values: Decimal, never Double>`, and change `review/SKILL.md:99` to `No magic numbers for <domain> thresholds — use named constants`. **Fixed 2026-09-24** — placeholder from AGENTS.md with money-as-`Decimal` as the example; `/review` says `<domain> thresholds`.
- [x] Pipeline gate coverage (check 5), row "`/review` before `/test` (not parallel)", required in `feature/SKILL.md` Done-when **and** AGENTS.md: `feature/SKILL.md:58` is correct. But the AGENTS.md that pragma generates for every consumer (`scripts/setup.sh:179-220`, which `/pragma:init` step 4 keeps "as-is" per `init.md:65`) never states the order. Its Merge rule (`scripts/setup.sh:219`) only lists the three conditions ("`/review` returns APPROVED, `/test` passes, and `code-review:code-review` is clean"), with no ordering and no pipeline line. FinanceTracker's own AGENTS.md has the equivalent line (`FinanceTracker/AGENTS.md:42`, "`/pr-followup` (auto-chains `/review` → `/test` → `code-review:code-review`)"). `design/SKILL.md:126` also assumes an "AGENTS.md/CLAUDE.md pipeline note" that the scaffold never creates. *Adapted: pragma has no AGENTS.md of its own, so the check was applied to the AGENTS.md template it generates.* — **Fix:** add one "Standard pipeline" line to the setup.sh AGENTS.md heredoc, stating `/review` → `/test` → `code-review:code-review` run in order (via `/pr-followup`), not in parallel. Keep the generated file ≤50 lines (it is 40 now). **Fixed 2026-09-24** — generated AGENTS.md has a Pipeline section with the order (44 lines, verified by a scratch `setup.sh` run).
- [x] Review-gate bypass for hotfixes. This is outside the rubric's fixed rows, and the severity is my judgment, by analogy with check 5's `/review` row. The scaffolded AGENTS.md Merge rule (`scripts/setup.sh:219`) exempts "`release/*`/`hotfix/*` PRs targeting `main`" from `/review` and `code-review:code-review` because "every commit already passed both when it merged into `develop`". That reason is false for hotfixes: `bugfix/SKILL.md:18` branches a hotfix off `main`, and `bugfix/SKILL.md:80` says those commits "have never been through `/review` on `develop`" and must go through `/review`/`/test`/`code-review:code-review`. An agent that reads AGENTS.md (always loaded) sees an exemption that the on-demand bugfix skill contradicts. FinanceTracker's AGENTS.md:47 has the same wording. — **Fix:** narrow the exemption to `release/*` only in `scripts/setup.sh:219`, and state that `hotfix/*` PRs to `main` need `/review` + `/test` + `code-review:code-review`. Apply the same change to `FinanceTracker/AGENTS.md:47` (cross-project check). **Fixed 2026-09-24** — exemption narrowed to `release/*` in `setup.sh` and in `FinanceTracker/AGENTS.md`. Related, found while fixing: both repos' `gates` CI job skipped hotfix PRs to `main` on the same false premise; it now runs for `hotfix/*` (it diffs against `origin/$BASE_REF`, so only the hotfix is checked).

## Medium
- [x] Command file completeness (check 6): `deterministic-pr-gates/SKILL.md` has no **Trigger**, **Process**, or **Done when** heading (its headings are "Why deterministic…", "When gates apply", Gate 0–10, "Gate summary", "Instantiating…", "Relationship…"; see `deterministic-pr-gates/SKILL.md:8-170`). It came into this check's scope when it moved from `skills/` to `.claude/skills/` in the AGENTS.md/skills migration. The 2026-09-09 review treated it as a local-skill reference, not a pipeline command. — **Fix:** decide which applies. (a) Add minimal Trigger/Process/Done when headings (e.g. rename "When gates apply" → "Trigger" and add a one-line Done when). (b) Record in `pipeline-review/SKILL.md` check 6 that this standalone skill is exempt, the same way `pragma-review.md:48` and `sync-workflow/SKILL.md:82-84` already exempt it from gate-count checks. **Fixed 2026-09-24** — option (a), Akshay's choice: Trigger, Process and Done when sections added; description unchanged.
- [x] Settings hygiene (check 7), "Missing `UserPromptSubmit` hook for unaddressed pipeline-review alert": pragma's installer registers only the `PreToolUse` guard hook (`scripts/install_guard_hook.py:36-43`, `:103-105`). No scaffolded file anywhere in pragma defines a `UserPromptSubmit` hook: a repo-wide grep finds the string only in `pipeline-review/SKILL.md:67` itself. So a consumer gets the `addressed: false` convention (`pipeline-review/SKILL.md:77`, `status/SKILL.md:58`), but nothing surfaces it at prompt time. For reference, FinanceTracker's `.claude/settings.json` has a working one. *Adapted: pragma has no `.claude/settings.json`, so the check was applied to the settings the installers write into a consumer project.* — **Fix:** either ship an optional, cwd-relative `UserPromptSubmit` alert hook via `install_guard_hook.py` (or a sibling installer), or change check 7 to say the hook is optional and that `/status` step 6 is the canonical surface. **Fixed 2026-09-24** — shipped: `scripts/install_review_alert_hook.py`, wired into `setup.sh` (`--no-review-alert`) and `/pragma:init`. Tested: 6 report cases, idempotent re-run, stale-entry replacement, invalid JSON left alone.
- [x] Template drift (check 2), template → project (the live pipeline is running stale logic). `/feature`'s **doubt-driven review** step (`pragma feature/SKILL.md:27`: a fresh-context subagent reviews the diff for new protocols or `@Model` mutations before the GREEN commit, and real findings are logged to incidents.md) is absent from `FinanceTracker/.claude/skills/feature/SKILL.md` (its per-task rules go straight from the `simplify` line to the CHANGELOG line). — **Fix:** port the bullet into FinanceTracker's feature skill, adjusting the gate numbers to FinanceTracker's own (Gate 10 = abstraction bloat, Gate 9 = architecture). **Fixed 2026-09-24** — in the FinanceTracker PR.
- [x] Template drift (check 2), template → project: the hotfix back-merge rule (`pragma bugfix/SKILL.md:79-80`: both hotfix PRs go through `/review`/`/test`/`code-review:code-review`, because hotfix commits never passed review on `develop`) is missing from `FinanceTracker/.claude/skills/bugfix/SKILL.md:79`, which only says "The Review Agent (`/review`) runs on the PR." — **Fix:** port pragma's two-line Regular/Hotfix split into FinanceTracker's bugfix skill. **Fixed 2026-09-24** — in the FinanceTracker PR.
- [x] Template drift (check 2), template → project: two `/review` improvements are missing from `FinanceTracker/.claude/skills/review/SKILL.md`. (1) Exit 2 from `check_tdd_commit_order.py` must be reported as "template default, not a pass" (`pragma review/SKILL.md:65-66`). (2) The advisory layout-adaptivity / `#available` design check (`pragma review/SKILL.md:92`). — **Fix:** port both into FinanceTracker's review skill. **Fixed 2026-09-24** — both ported in the FinanceTracker PR.
- [x] Template drift (check 2), template → project: FinanceTracker's Gate 2 counts executed tests only in XCTest format. `FinanceTracker/.claude/skills/gates/SKILL.md:71` greps `^Test [Cc]ase '.*' passed` only, while pragma's Gate 2 (`gates/SKILL.md:73`) also matches Swift Testing's `✔ Test "…" passed` lines. FinanceTracker's unit tests are Swift Testing, so its Gate 2 executed-count and failure count miss every unit test. FinanceTracker's own `test/SKILL.md:71` and `release/SKILL.md:27` already use pragma's pattern, so only its gates skill is behind. pragma's Gate 1 Xcode 27 SDK-migration advisory (`gates/SKILL.md:61-64`) is also missing from FinanceTracker's gates skill, and FinanceTracker is on Xcode 27. — **Fix:** port pragma's Gate 2 grep line and the Gate 1 advisory into FinanceTracker's gates skill. **Fixed 2026-09-24** — both ported in the FinanceTracker PR. Correction: "its Gate 2 executed-count and failure count miss every unit test" is false on Xcode 27, which prints `Test case '…' passed` for Swift Testing (191 vs 0 `✔` lines on the 2026-09-24 run). pragma's own Gate 2 note made the opposite wrong claim and is corrected in this PR.
- [x] Template drift (check 2), template → project: FinanceTracker's `gates` and `release` skills have **two frontmatter blocks**. `FinanceTracker/.claude/skills/gates/SKILL.md:1-9` and `release/SKILL.md:1-9` put `model: claude-haiku-4-5-20251001` in a second `---` block after the real frontmatter closes at line 5. Only the first block is frontmatter, so the Haiku routing is probably not applied and those two skills run on the session model. pragma's copies are correct (`gates/SKILL.md:1-6`, `release/SKILL.md:1-6`). — **Fix:** in FinanceTracker, move `model:` into the first frontmatter block of both files and delete the stray `---`/`model:`/`---` lines. **Fixed 2026-09-24** — in the FinanceTracker PR.
- [x] Memory staleness (check 4): `project_xcode27_agent_skills_pending.md` says the Xcode 27 Agent Skills work is "Blocked on Xcode 27 actually being installed, which hasn't happened yet". Xcode 27 is installed: `xcodebuild -version` → `Xcode 27.0 / Build version 27A266a`. FinanceTracker's gates skill also records a check "verified 2026-09-20 (Xcode 27 / Device Hub)" (`FinanceTracker/.claude/skills/gates/SKILL.md:332`). The MEMORY.md index line repeats "blocked until Xcode 27 installed". — **Fix:** mark the blocker cleared and re-add the Backlog card the memory says to create "once Xcode 27 is installed" (`test-modernizer`, `swiftui-whats-new-27` export and evaluation). Update the index line. **Resolved 2026-09-24** — see the FinanceTracker report: export command absent in Xcode 27.0; memory updated, no card (Akshay).
- [x] Memory staleness (check 4): `feedback_auto_sync_workflow.md` triggers `/sync-workflow` "after any change to `.claude/commands/` files, `CLAUDE.md`". After the skills/AGENTS.md migration, the pipeline lives in `.claude/skills/` and `AGENTS.md`, and FinanceTracker's own sync-reminder hook now matches `.claude/skills/` (`FinanceTracker/.claude/settings.json`, PostToolUse). A literal reading of the memory would miss every skill edit. — **Fix:** change the trigger to `.claude/skills/*/SKILL.md`, `AGENTS.md`/`CLAUDE.md`, and branch/build conventions. **Fixed 2026-09-24**.
- [x] Memory staleness (check 4): `feedback_code_review_scope.md` scopes the "always run `code-review:code-review`" rule to PRs touching `.claude/commands/` or `.claude/commands/*.md`. The pipeline's command files are now `.claude/skills/*/SKILL.md` in both repos (pragma keeps only `init.md` and `pragma-review.md` under `.claude/commands/`). — **Fix:** change the scope to `.claude/skills/`, `.claude/commands/`, `scripts/`, `scaffold/`, and `.claude/hooks/`. **Fixed 2026-09-24**.
- [x] Memory staleness (check 4): `feedback_gh_pr_create_head_flag.md` says the rule is codified in "FinanceTracker's CLAUDE.md ('Cross-repo rule')" and "pragma's own `sync-workflow.md`". FinanceTracker's CLAUDE.md is now a one-line `@AGENTS.md` import, and the rule lives at `FinanceTracker/AGENTS.md:49`. pragma's copy is now `.claude/skills/sync-workflow/SKILL.md:117`. — **Fix:** update both citations. **Fixed 2026-09-24**.
- [x] Memory staleness (check 4): `project_root_launch_migration.md` contradicts itself. Line 198 says project `.claude/settings.json` hooks firing in root-launched sessions is "NOT yet empirically verified". Lines 206-208 of the same file record it as tested and confirmed (they do not load), and the 2026-09-24 addendum builds on that. Lines 208-213 also still describe `.claude/commands/*.md` slash commands and "pragma's own `pipeline-review.md` command", which are now skills. — **Fix:** delete or strike the line-198 bullet as superseded, and change the commands references to `.claude/skills/`. **Fixed 2026-09-24**.
- [x] Memory staleness (check 4): `feedback_pr_workflow_discipline.md` §3 says that for any pragma edit, "Branch name convention: `sync/YYYY-MM-DD`." pragma's merge history contradicts this: the last 22 merged PRs mostly use `chore/*`, `fix/*`, `ci/*`, `docs/*` and `release/*`, with `sync/*` only for `/sync-workflow` runs (e.g. `chore/pragma-review-sonnet-5`, `fix/changelog-gate-integrity-count`, `release/2.0.0`). §4 also cites "CLAUDE.md's Merge rule", which is now in AGENTS.md. — **Fix:** limit `sync/YYYY-MM-DD` to `/sync-workflow` runs, point other pragma changes to `chore/*`/`fix/*`, and change the Merge-rule citation to AGENTS.md. **Fixed 2026-09-24** — `sync/YYYY-MM-DD` only for `/sync-workflow` runs (Akshay confirmed); Merge-rule citation now AGENTS.md.
- [x] Memory staleness (check 4): `feedback_token_optimization.md` says "Keep CLAUDE.md under 50 lines" and "CLAUDE.md does NOT belong: anything already in `.claude/commands/`". For the pipeline projects, the budgeted file is now AGENTS.md (FinanceTracker's CLAUDE.md is a one-line import, and pragma's `pipeline-review`/`trim-context` skills target AGENTS.md), and the duplicated content now lives in `.claude/skills/`. The MEMORY.md index line has the same wording. — **Fix:** reword to "AGENTS.md (or CLAUDE.md for non-migrated projects) ≤50 lines; don't duplicate `.claude/skills/`". **Fixed 2026-09-24**.

## Low
- [x] Template drift (check 2), project → template back-port. **This is a live contradiction inside pragma itself, not just a missing enhancement.** `gates/SKILL.md:313-320` tells the agent to append a `[CANDIDATE]` comment to `.claude/context/invariants.md` and commit it. But `/gates` normally runs on a `feature/*` branch, where pragma's own guard hook blocks that write (`scaffold/.claude/hooks/guard_protected_paths.py:57`) and Gate 11 flags it (`scripts/check_gate_integrity.py:85`; also described at `gates/SKILL.md:355`). FinanceTracker already fixed this: on `feature/*`, put the candidate under `## Candidate invariants` in the PR body instead (`FinanceTracker/.claude/skills/gates/SKILL.md:388-389`). — **Fix:** back-port FinanceTracker's two-bullet branch split into pragma's "Write candidate invariants" section. The rubric rates this direction Low; consider fixing it first among the Lows. **Fixed 2026-09-24** — back-ported (Gate 11 numbering).
- [x] Template drift (check 2), back-port: Gate 11 does not say what to do when `scripts/check_gate_integrity.py` is missing (`gates/SKILL.md:232-258`). FinanceTracker makes that an explicit FAIL, "never skip it as 'not applicable'" (`FinanceTracker/.claude/skills/gates/SKILL.md:374-375`). — **Fix:** add that sentence to pragma's Gate 11. **Fixed 2026-09-24**.
- [x] Template drift (check 2), back-port: pragma's Gate 8 protocol detector (`gates/SKILL.md:128`, `"^protocol \|^public protocol "`) misses `internal`/`package`/`private`/`fileprivate`/`open`/`nonisolated` or attribute-prefixed declarations (e.g. `@MainActor protocol`). FinanceTracker's version handles those modifiers (`FinanceTracker/.claude/skills/gates/SKILL.md:288`). — **Fix:** port FinanceTracker's `grep -Eln` modifier-aware pattern. **Fixed 2026-09-24**.
- [x] Template drift (check 2), back-port: Gate 8 is "advisory… do not block the PR" (`gates/SKILL.md:137`), but the `/loop` stop conditions include "no abstraction bloat/duplication" (`gates/SKILL.md:302`, `feature/SKILL.md:66`), and Done when says "All 11 gates pass" (`gates/SKILL.md:361`, `feature/SKILL.md:58`). An unattended loop would try to remove advisory candidates. FinanceTracker separates blocking gates from advisory ones (`FinanceTracker/.claude/skills/gates/SKILL.md:277`, `:437-438`). — **Fix:** change the loop condition to "all blocking gates pass (Gate 8 advisory)" and change Done when to match. **Fixed 2026-09-24** — loops and Done when say "all blocking gates"; the loop no longer asks to remove abstraction candidates.
- [x] Template drift (check 2), back-port: Gate 5's remedy (`gates/SKILL.md:108`) says to add "a one-line summary per task commit". FinanceTracker refines this to summarize GREEN commits only, because RED (test-only) commits have nothing user-facing (`FinanceTracker/.claude/skills/gates/SKILL.md:100-103`). — **Fix:** port the refinement. **Fixed 2026-09-24**.
- [x] Template drift (check 2), back-port: `/release`'s "No force-unwraps in production code added since last release" pre-flight has no command (`release/SKILL.md:30`). FinanceTracker gives one (`FinanceTracker/.claude/skills/release/SKILL.md:36-39`). — **Fix:** add a templated command, ideally reusing Gate 10's `try!`/`as!` grep (`gates/SKILL.md:207`) over `git diff <last-tag>..develop`. **Fixed 2026-09-24**.
- [x] Template drift (check 2, CI workflows per `sync-workflow/SKILL.md:47`), back-port: the scaffold's `gates` CI job has no base-branch guard. `scaffold/.github/workflows/pr-checks.yml` triggers on `[develop, main]` (line 21) and runs `gates` for both, so a `release/*` PR to `main` re-checks the whole release delta. FinanceTracker restricts the job to `develop` with `if: github.base_ref == 'develop'` and documents why (`FinanceTracker/.github/workflows/pr-checks.yml:94-98`). — **Fix:** port the `if:` guard and its comment. **Fixed 2026-09-24** — ported, with a `hotfix/*` exception (see the hotfix High above); FinanceTracker's comment corrected too.
- [x] Template drift (check 2), back-port: pragma's `pipeline-review/SKILL.md` still says "command file" after the skills migration (`:26` "used in a command file", `:48` "exist as command files", `:60-61` "### 6. Command file completeness"). FinanceTracker's copy says "skill file". — **Fix:** reword to "skill file" (or "skill or command file", since `init.md`/`pragma-review.md` remain commands). **Fixed 2026-09-24** — "skill file" / "skill or command file".
- [x] Template drift (check 2), back-port: `benchmark/SKILL.md:14` omits "a `model:` frontmatter change" from its triggers, even though pragma's gates/release skills use `model:` routing. FinanceTracker includes it. — **Fix:** add it back. **Fixed 2026-09-24**.
- [x] Template drift (check 2), back-port: `trim-context/SKILL.md:57-59` derives the transcript path from `pwd`. That is wrong when the session is launched from a parent directory, a setup pragma itself acknowledges at `gates/SKILL.md:358`. FinanceTracker documents why it doesn't use `pwd` (`FinanceTracker/.claude/skills/trim-context/SKILL.md:59`). — **Fix:** add a one-line caveat: use the launch directory, not `pwd`, if sessions start above the project root. **Fixed 2026-09-24** — caveat added.
- [x] Template drift (check 2): FinanceTracker has a Gate 12 "Visual verification" (advisory screenshot via XcodeBuildMCP, `FinanceTracker/.claude/skills/gates/SKILL.md:321-359`) with no template equivalent. It depends on XcodeBuildMCP, so it may be FinanceTracker-specific under `sync-workflow/SKILL.md:52-54`. — **Fix:** decide whether it generalizes. If it does, port it as an optional, `[i]`-only templated gate. If not, record it as app-specific so future reviews stop re-raising it. **Deferred 2026-09-24** — not ported: it depends on an MCP server the project installs itself, so it is app-specific. `/sync-workflow`'s App-specific bullet now names that case so reviews stop re-raising it.
- [x] Outside the 7 checks (found while reading): the `/status` phase table (`status/SKILL.md:25-33`) has no row for `hotfix/*` or `chore/*`, although Gate 4 accepts both (`gates/SKILL.md:100`) and pragma's own history mostly uses `chore/*`. `/status` would report no phase on those branches. — **Fix:** add `hotfix/*` → `/bugfix` (hotfix) and `chore/*` → maintenance rows. **Fixed 2026-09-24**.

## All clear
- Stale skill references (check 1): every skill or command invoked in the 17 `.claude/skills/*/SKILL.md` files and 2 `.claude/commands/*.md` files is on the registry at `pipeline-review/SKILL.md:24`: `/gates`, `/review`, `/feature`, `/test`, `/pr-followup`, `/spec`, `/loop`, `/design`, `/release`, `/bugfix`, `/sync-workflow`, `/plan`, `/pipeline-review`, `/status`, `/pragma-review`, `/parallel-review`, `/benchmark`, `/trim-context`, `simplify`, `ios-coverage`, `security-review`, `code-review:code-review`, `deterministic-pr-gates`. `/pragma:init` and `/pragma:pragma-review` are the plugin-prefixed forms of `init`/`pragma-review`. `/babysit` appears only in a note saying no such command exists (`review/SKILL.md:160`). Every one of the 17 skill directories is itself on the registry.
- AGENTS.md token budget (check 3): pragma has no AGENTS.md/CLAUDE.md of its own. *Adapted:* counted the AGENTS.md that `scripts/setup.sh` generates for consumers (heredoc at `scripts/setup.sh:180-219`): 40 lines, under the 50-line target.
- Pipeline gate coverage (check 5), all rows except the AGENTS.md half of the `/review`→`/test` row (see High): `/gates` exists (`gates/SKILL.md`); build is verified separately from tests (Gate 1 `gates/SKILL.md:44` vs Gate 2 `:66`); CHANGELOG is updated incrementally in `/feature` (`feature/SKILL.md:28`); `/review` before `/test` is in `feature/SKILL.md:58`; coverage uses `ios-coverage` (`gates/SKILL.md:114`); security uses `security-review` (`gates/SKILL.md:122`); session recovery is `status/SKILL.md`.
- Command file completeness (check 6): all 16 pipeline skills and both commands (`init.md`, `pragma-review.md`) have Trigger, Process and Done when. The only exception is `deterministic-pr-gates` (see Medium).
- Settings hygiene (check 7), hooks referencing non-existent files: *adapted to the hook the installers register.* Its command points at `$CLAUDE_PROJECT_DIR/.claude/hooks/guard_protected_paths.py`, which `install_guard_hook.py` copies from `scaffold/.claude/hooks/` (present) before registering, and it exits 0 if the file is absent (`install_guard_hook.py:38-42`). `python3 scaffold/.claude/hooks/guard_protected_paths.py --self-test` passes: 0 failures, and the hook's globs match `check_gate_integrity.py`'s `GUARDED_PATH_GLOBS`. Overly broad permissions: not applicable, because pragma scaffolds no `permissions` block.
- Gate numbering/count consistency (the deterministic check from `sync-workflow/SKILL.md` 5c, run as a spot-check): Gates 1–11 are sequential, `MAX=11`, and no "N gates" reference in `.claude/skills/`, README.md or CONTRIBUTING.md disagrees.
- Memory references to PRs/branches (check 4): every PR cited in memory resolves on GitHub. pragma: #32 closed, #46/#47/#48 merged. FinanceTracker: #48/#49/#60/#61/#92/#93/#94/#99/#100 merged. Dimillian/Skills #17 is open, as the memory says. No memory file was last modified more than 60 days ago (oldest mtime 2026-08-30), so the ">60 days unconfirmed" criterion flagged nothing.

## Scope and adaptation notes
- pragma is the template, not a derived app. For check 2, "this project" was taken to be FinanceTracker (the only project derived from pragma, same as the 2026-09-09 review). Each `FinanceTracker/.claude/skills/*/SKILL.md` was diffed against pragma's after substituting `FinanceTracker` → `<AppName>`, and FinanceTracker's CI workflows and guard hook were compared with `scaffold/`. Both repos were fetched and even with `origin/develop` at audit time. FinanceTracker has no skill without a template equivalent, so the "project-only skill file" (Medium) direction found nothing.
- Check 4: pragma's own project-keyed memory store (`~/.claude/projects/-Users-akshaypimprikar-Desktop-Claude-pragma/memory/`) exists but is empty. Sessions launch from `~/Desktop/Claude`, so the live store is `~/.claude/projects/-Users-akshaypimprikar-Desktop-Claude/memory/` (85 files incl. MEMORY.md). The 12 files that concern pragma or the pipeline were read in full. The other 73 (LinkedIn, job search, reports, tooling) were scanned for PR, branch, command-path and file-path references, and every hit was checked (the `~/.claude/commands/agentic-ai-report.md`-style references resolve).
- Not re-raised because they are already tracked: open PR #72 (README plain-English rewrite, stale/conflicting, to be redone) and the deliberately omitted cross-tool "compatible" README badges (backlog card). The gate-numbering divergence between pragma and FinanceTracker (Deferred 2026-09-09) still exists and is still deferred. It also explains the "Gate 9"/"Gate 11"/"13 gates" differences seen in FinanceTracker's skills; those were not counted as new findings.
- PushNotification step skipped at the requester's instruction; results relayed by the caller.

## Resolution (2026-09-24)
All 30 items are fixed or deferred with a reason. Found while fixing and fixed in the same PRs: pragma's Gate 2 note claimed `xcodebuild test` never prints `Test case '…' passed` for Swift Testing (false on Xcode 27); both repos' `gates` CI job skipped `hotfix/*` PRs; two memory links used `feedback-present-before-fixing` instead of `feedback_present_before_fixing`.
