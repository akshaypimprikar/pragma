---
date: 2026-09-07
addressed: true
---

# Pipeline Review — 2026-09-07

## Critical
- [x] `pipeline-review.md`'s own valid-skills registry omits four command files that live in `.claude/commands/` and are actively referenced elsewhere in the pipeline: `benchmark` (self-invoked `benchmark.md:8`), `parallel-review` (self-invoked `parallel-review.md:8`), `pr-followup` (invoked `gates.md:260`, `test.md:6`, self `pr-followup.md:15`), `pragma-review` (invoked `init.md:27`, self `pragma-review.md:16`) — `pipeline-review.md:18` — **Fix:** add these four names to the registry line (or, for `pragma-review`/`init`, explicitly document they're excluded as pragma-repo-only meta-commands per `init.md:27`, mirroring how they're already handled there). **Resolved:** added `benchmark`, `parallel-review`, `pr-followup`, `pragma-review` to the registry line.
- [x] `review.md` references the skill as `/code-review` (bare), which does not match the registry's `code-review:code-review` entry — `review.md:61` — **Fix:** change `/code-review` to `` `code-review:code-review` `` for consistency with the registry and with every other reference to this skill in the codebase. **Resolved:** fixed at `review.md:61`.

## High
(none)

## Medium
- [x] Memory `project_pragma_visibility_push.md` (modified 2026-09-01) claims the 2026-08-17 `/pipeline-review` run's Critical "stale `/code-review` skill reference in `review.md`" finding was resolved by pragma PR #48 — verified via `gh pr view 48 --json files` that PR #48 only touched `CHANGELOG.md` and `scripts/setup.sh` (a `setup.sh` OS-pin backport, an unrelated Low finding). `review.md:61` still contains the bare `/code-review` reference today, confirming the fix never actually landed — `project_pragma_visibility_push.md:23` — **Fix:** correct the memory to state this finding was never fixed, and land the `review.md:61` fix (see Critical item above) for real. **Resolved:** memory corrected with a 2026-09-07 note; `review.md:61` fix landed above.
- [x] `feature.md:51` and `gates.md:261-262` both assert `code-review:code-review` "can't be agent-invoked" / "still has to be run manually," which contradicts the confirmed, current operating record in memory `feedback_pr_gate_execution.md` (modified 2026-08-19, Akshay-confirmed on PR #100): "always run review and test on PRs" and specifically that `code-review:code-review`'s instructions load inline via the Skill tool the same as `/review` — it is not manual-only. This may be intentionally scoped to autonomous `/loop`/subagent contexts that lack Skill-tool access rather than a plain error — needs a human call on which framing is intended — `feature.md:51`, `gates.md:261-262` — **Fix:** reconcile the wording — either clarify "manual only within an autonomous `/loop` run; top-level sessions should invoke it directly," or drop the "manual" framing entirely if it no longer applies anywhere. **Resolved (Akshay's call, 2026-09-07): always run it automatically.** Updated `feature.md:51`, `gates.md` tip, `pr-followup.md` (now auto-chains `/review` → `/test` → `code-review:code-review`), and `parallel-review.md` (same fix, for consistency) to run it automatically by default, with a documented exception only for projects that have `disable-model-invocation` set on that skill.

## Low
- [x] Memory `feedback_check_remote_sync_before_branching.md` cites commit `f17bc6a` and branches `feature/parallel-review-command`/`chore/goal-loop-docs` as the incident that motivated the rule; none of these are found in pragma's git history (`git cat-file -t f17bc6a` fails, no matching local/remote branch, no PR with those head-branch names in `gh pr list`). Low confidence this is actually stale rather than out-of-scope: the memory doesn't name a repo, and the incident's branch names/commit don't match anything in pragma's `gh pr list` — it may describe a FinanceTracker-repo event instead — `feedback_check_remote_sync_before_branching.md:12-14` — **Fix:** when next touching this memory, confirm which repo the incident occurred in and note it explicitly to make future cross-checks unambiguous. **Resolved:** confirmed via FinanceTracker's git history (commit and both PRs #48/#49 exist there) — memory updated to name FinanceTracker explicitly.

## All clear
- Template drift (check 2): not applicable in the literal sense — pragma is the template/workflow source itself, with no downstream consumer project in this repo to diff against. Spot-checked all `.claude/commands/*.md` for leaked app-specific content: every generic command correctly uses `<AppName>`-style placeholders (`design.md`, `spec.md`, `plan.md`, `feature.md`, `gates.md`, `review.md`, `test.md`, `bugfix.md`, `sync-workflow.md`, `status.md`, `release.md`); the only literal "FinanceTracker" mentions are in `pragma-review.md`, which is explicitly scoped to the pragma repo only (guarded at `pragma-review.md:20-24`) and correctly excluded from the template-copy set per `init.md:27`. No placeholder leaks found.
- CLAUDE.md token budget (check 3): not applicable — pragma has no `CLAUDE.md` of its own. By design, `scripts/setup.sh` (lines 113-162) generates a starter `CLAUDE.md` into consumer projects; pragma itself is the scaffold, not an app.
- Memory staleness (check 4): no memory is older than 60 days without reconfirmation (oldest modified date found: `feedback_visuals_in_pr_descriptions.md` at 2026-07-24, 44 days old). Spot-verified several specific claims as accurate and current: `feedback_gh_pr_create_head_flag.md`'s account of pragma PR #32 (confirmed CLOSED, opened from `develop` instead of `release/1.0.0`, matches `gh pr list` exactly); `project_pragma_visibility_push.md`'s claim that Dimillian/Skills PR #17 is still open (confirmed OPEN via `gh pr view`); `project_pragma_visibility_push.md`'s Gate 9 RED-before-GREEN and `/pragma-review` additions (both confirmed present in `gates.md:92` and `pragma-review.md`).
- Pipeline gate coverage (check 5): all seven required gates exist at their required locations — Pre-PR gate (`gates.md`), Build verification separate from tests (`gates.md:27` Gate 1 vs. `gates.md:35` Gate 2), CHANGELOG incremental update (`feature.md:21`), `/review` before `/test` ordering (`feature.md:51`, content flagged separately above), coverage check (`gates.md:67` references `ios-coverage`), security check (`gates.md:75` references `security-review`), session recovery (`status.md`).
- Command file completeness (check 6): all 18 files in `.claude/commands/` have Trigger, Process, and Done when sections — no gaps found.
- Settings hygiene (check 7): not applicable — no `.claude/settings.json` exists in the pragma repo. Pragma is a template/plugin source distributed via `.claude-plugin/`; it does not check in a settings file for itself (settings.json is machine/project-specific and generated per consumer project, not by pragma's own setup path).
